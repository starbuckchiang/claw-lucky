請更新 `gift.html` 的禮物資料，讓我提供的兩張新圖片與對應商品正確顯示。
圖片檔名:
image/tabletennis.jpg
image/pipipi.jpg  

工作要求：

1. 先檢查專案目前 `gift.html` 的資料來源、圖片欄位及 Supabase `gifts` 資料表結構。
2. 確認兩張新圖片的實際檔名、路徑與大小寫，不可自行猜測。
3. 將兩張圖片加入正確的圖片目錄；若圖片已存在，不要建立重複檔案。
4. 檢查 Supabase `gifts` 資料表資料，使用現有欄位與資料格式，不可另建重複商品。
5. 如果專案以 migration 或 seed 管理資料，請新增可重複安全執行的 SQL；優先使用 `UPDATE` 或 `UPSERT`，避免刪除既有資料。
6. 確認 `gift.html` 使用的圖片欄位與資料庫一致，必要時修正圖片 URL 組合邏輯。
7. 不要破壞既有的兌換、點數扣除、庫存及兌換紀錄功能。
8. 若缺少商品名稱、點數、gift ID 或兩張圖片的對應關係，先列出缺少資料並停止，不可自行假設。
9. GitHub安全提交

驗收標準：

* `gift.html` 可正常載入。
* 兩張新圖片分別顯示在正確的禮物項目。
* 圖片沒有 404、破圖或路徑大小寫錯誤。
* 資料庫沒有產生重複商品。
* 原有禮物與兌換功能仍可正常使用。
* 提供實際執行的 SQL、修改檔案清單、測試結果，以及需要我手動執行的部署步驟。
